apiVersion: v1
kind: Service
metadata:
  name: quotes-service
  namespace: quoteapp
  labels:
    app: quotes
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: quotes
    tier: backend
